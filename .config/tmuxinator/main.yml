# /home/dima/.config/tmuxinator/main.yml

name: main
root: ~/

windows:
  - main:
      layout: even-horizontal
  - task:
      layout: main-vertical
      panes:
        - task
  - buku:
      layout: main-vertical
